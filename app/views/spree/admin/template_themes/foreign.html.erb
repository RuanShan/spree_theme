<%= render :partial => 'sub_menu' %>
<h1>Foreign themes</h1>

<table>
  <tr>
    <th>TemplateTheme</th>
  </tr>

<% @themes.each do |theme| %>
  <tr>
    <td><%= theme.title %></td>

    <td><%= link_to 'Preview', preview_template_theme_path(theme) %></td>
    <td><%= link_to 'Import', release_admin_template_theme_path(theme), {:method=>:post} %></td>
  </tr>
<% end %>
</table>

<br />

